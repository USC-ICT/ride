fastapi
# NOTE (license): piper-tts 1.3.0+ is the OHF-Voice/piper1-gpl line, licensed GPL-3.0-or-later.
# The app uses `python -m piper` and `python -m piper.download_voices`, which exist only in this
# (GPL) line — the last MIT release, 1.2.0, lacks them. Pinned for reproducibility; the GPL posture
# of this optional example service is an open decision (see WebServices/LOCAL-AI-SERVICES.md §3.1).
piper-tts==1.4.2
uvicorn[standard]
